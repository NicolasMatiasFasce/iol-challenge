# rate-limiter

This folder is included to comply with the challenge submission format.

Required design document:

- `DESIGN.md`

`DESIGN.md` is a symlink to `../openspec/changes/add-http-rate-limiter/design.md`
to keep a single source of truth and avoid divergence between docs.

Implementation is in:

- `../iol-challenge/`

